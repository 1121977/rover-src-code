# HTTP Basic for /api; override per environment (encoding prefix required, e.g. {bcrypt}...).
app.security.user.name=user
app.security.user.password={bcrypt}$2a$10$dXJ3SW6G7P50lGmMkkmwe.20cQQubK3.HZWzG3YB1tlRy.fqvM/BG

jpa.connection.url=jdbc:h2:file:./webapps/rover/WEB-INF/classes/data/rover-db
# Override in deployment; defaults match existing embedded H2 file created with sa/sa.
jpa.connection.username=${JPA_CONNECTION_USERNAME:sa}
jpa.connection.password=${JPA_CONNECTION_PASSWORD:sa}
path.dir.photo=webapps/rover/WEB-INF/classes/photo
path.dir.data=webapps/rover/WEB-INF/classes/data